export { FONT_FILE_NAME_REGEX, ITALIC_REGEX, VARIABLE_FONT_REGEX } from '#font';

export {
  EMAIL_REGEX,
  LETTERS_WITH_SPACES_REGEX,
  NOT_SEPARATE_REGEX,
  NOT_SPACE_REGEX,
  extensions,
  numbers,
  phones,
  selectors,
} from '#regex';
