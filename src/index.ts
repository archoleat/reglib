export { FONT_FILE_NAME_REGEX, ITALIC_REGEX, VARIABLE_FONT_REGEX } from '#font';

export {
  EMAIL_REGEX,
  LATIN_AND_CYRILLIC_WITH_SPACES_REGEX,
  NOT_SEPARATE_REGEX,
  NOT_SPACE_REGEX,
  extensions,
  numbers,
  phones,
  selectors,
} from '#regex';
